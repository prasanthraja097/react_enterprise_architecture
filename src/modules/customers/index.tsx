import React from 'react'

const CustomersModule: React.FC = () => {
  return (
    <div>
      <h1>Customers</h1>
      <p>Placeholder customers module — follow the same module pattern as `auth`.</p>
    </div>
  )
}

export default CustomersModule
